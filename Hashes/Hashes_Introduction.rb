# Hashes are optimal for storing relationships within objects
# Store key value pairs
# Dictionary is the best comparison of hash like the work is the identifies and the value the definition 
#Hash key cannot be repeated but the value can  and it should be used as reference point and not by the numerical order

this_is_a_hash = {}
p this_is_a_hash    #{}
p this_is_a_hash.class  #Hash 